import 'package:flutter/material.dart';

class HomeTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // Customize the content of your HomeTab widget here
      child: const Text('Home Tab Content'),
    );
  }
}